# React Frontend

This is the frontend for the project, built with React.js.

## Getting Started

1. Install dependencies:
   ```bash
   npm install
   ```
2. Start the development server:
   ```bash
   npm start
   ```

The app will run on http://localhost:3000 by default.
